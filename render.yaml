services:
  - type: web
    name: prom-parser
    env: node
    buildCommand: apt-get install -y chromium-browser && npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
